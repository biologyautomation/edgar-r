% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/py_random.R
\name{edgar_py_random}
\alias{edgar_py_random}
\title{Constructor: returns an environment that owns the MT state. Pass by
reference lets \code{genrand_uint32} advance the state without copying.}
\usage{
edgar_py_random(seed)
}
\arguments{
\item{seed}{Integer (or numeric coercible to one). Negative seeds are
silently treated as their absolute value, matching CPython's behaviour
where \code{random.Random(-1)} and \code{random.Random(1)} produce identical
streams.}
}
\value{
An environment with \verb{$state} (numeric vector of 624 uint32s) and
\verb{$index} (1-based pointer into \verb{$state}).
}
\description{
Constructor: returns an environment that owns the MT state. Pass by
reference lets \code{genrand_uint32} advance the state without copying.
}
